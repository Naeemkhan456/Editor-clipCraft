import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.editorclipcraft',
  appName: 'Editor-ClipCraft',
  webDir: 'dist'
};

export default config;
